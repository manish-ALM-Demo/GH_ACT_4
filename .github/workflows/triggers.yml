name: trigger

on:
  pull_request:
    types: [opened]
  workflow_dispatch:

jobs:
  activity:
    runs-on: manish_org_runner
    steps:
      - name: Print my statement
        run: echo "This trigger happened  "
